# teambition-client

Reusable Teambition Open API HTTP client extracted from the Streamlit app: default endpoint registry, dynamic `call()`, pagination helpers, and optional request logging hooks.

Used by `apps/backend` (FastAPI). The legacy Streamlit `app.py` can be migrated to import this package later.
