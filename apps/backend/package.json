{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=../../packages/teambition-client/src python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "echo \"Python: run ruff/flake8 in CI\"",
    "build": "echo \"no-op\""
  }
}
